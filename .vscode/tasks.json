{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build & copy game",
            "type": "shell",
            "command": "make && cp -r release/* /usr/share/games/quake2/"
        }
    ]
}